```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST request containing the new note
    activate server

    Note right of server: Server saves the note

    server-->>browser: JSON response

    deactivate server

    Note right of browser: JavaScript displays the new note without reloading the page
```