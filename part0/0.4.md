```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST request containing the new note
    activate server

    Note right of server: Server saves the note

    server-->>browser: Response containing the note

    deactivate server

    Note right of browser: Browser displays the new note
```